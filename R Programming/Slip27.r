# Slip 27
# Write an R Program to calculate binary into Decimal of a given number.
install.packages("asbio")
library(asbio)
bin2dec(1010)
#asbio: - is Package, Contains functions from, Foundational Statistics and Applied Analyses for
#Biologists.
#bin2dec( )- This function is used to convert decimal values to binary values